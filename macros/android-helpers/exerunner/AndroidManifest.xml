<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- debuggable is required to enable showing of LogCat in release builds. Adding debuggable=true in build.gradle makes even the release
         build being built in debug mode, which is not something we want -->

    <application
        android:allowBackup="false"
        android:label="@string/app_name">
        <activity android:name=".RunActivity" android:screenOrientation="nosensor" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>
